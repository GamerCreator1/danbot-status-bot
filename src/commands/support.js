
const { MessageEmbed } = require("discord.js");
module.exports.run = async (client, message) => {
    // let support = client.embed().setTitle("Support").setDescription("This bot is developed by GamerCreator1#1466, contact him on discord, for help.")
    // message.channel.send(support)
  
};
module.exports.help = {
  name: "support",
  alias: []
}